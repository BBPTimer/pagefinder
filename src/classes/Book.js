export default class Book {
  constructor(id, title, authors, isbn, pageCount, thumbnail) {
    this.id = id;
    this.title = title;
    this.authors = authors;
    this.isbn = isbn;
    this.pageCount = pageCount;
    this.thumbnail = thumbnail;
  }
}
